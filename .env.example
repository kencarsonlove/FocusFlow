# FocusFlow uses Firebase Auth + Firestore.
# Configure Firebase in:
# - ios/Runner/GoogleService-Info.plist
# - android/app/google-services.json
#
# For login verification emails, install the Firebase Trigger Email extension
# and set its watched collection to: mail
